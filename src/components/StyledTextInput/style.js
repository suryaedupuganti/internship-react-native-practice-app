import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
  input: {
    height: 40,
    width: "80%",
    margin: 5,
    borderColor: "gray",
    borderWidth: 1,
  },
});

export default styles;
